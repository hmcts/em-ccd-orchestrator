# Test configuration
spring:
azure:
  app_insights_key: ${APPINSIGHTS_INSTRUMENTATIONKEY:false}
  application-insights:
    instrumentation-key: ${APPINSIGHTS_INSTRUMENTATIONKEY:dummy}

core_case_data:
  api:
    url: ${CCD_DATA_API_URL:http://localhost:4452}

notify:
  apiKey: ${NOTIFICATION_API_KEY:test-api-key-dummy}
