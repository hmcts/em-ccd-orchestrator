spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: false
      paths: /mnt/secrets/em-ccdorc
      aliases:
        microservicekey-em-ccd-orchestrator: S2S_KEY
        AppInsightsInstrumentationKey: APPINSIGHTS_INSTRUMENTATIONKEY
        GovNotifyAPIKey: NOTIFICATION_API_KEY
